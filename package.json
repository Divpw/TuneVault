{
  "name": "com.example.tunevault",
  "displayName": "TuneVault",
  "version": "1.0.0",
  "description": "A music player app for offline songs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --no-deprecation node_modules/.bin/netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Apache Cordova Team",
  "license": "Apache-2.0",
  "dependencies": {
    "netlify-cli": "^17.17.1"
  },
  "devDependencies": {
    "cordova-android": "^14.0.1",
    "cordova-plugin-file": "^8.1.3"
  },
  "cordova": {
    "platforms": [
      "android"
    ],
    "plugins": {
      "cordova-plugin-file": {
        "ANDROIDX_WEBKIT_VERSION": "1.4.0"
      }
    }
  }
}